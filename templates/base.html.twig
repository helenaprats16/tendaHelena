<!DOCTYPE html>
<html lang="ca">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Tenda Helena{% endblock %}</title>
    <link rel="icon" type="image/png" href="{{ asset('imgs/productes/favicon.png') }}">
    
    {# Bootstrap CSS des de CDN #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    {% block stylesheets %}
      <link href="{{ asset('styles/estils.css') }}" rel="stylesheet" />
    {% endblock %}  
  </head>
  <body>

    {# Incloem el menú com una plantilla parcial #}
    {{ render(controller('App\\Controller\\SeccioController::menu')) }}


    <main class="container my-4">
        {% block contingut %}
          <!-- Contingut específic de cada pàgina -->
        {% endblock %}
    </main>

    {# Incloem el peu de pàgina #}
    {% include 'partials/_peu.html.twig' %}

    {# Bootstrap JS des de CDN #}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    {% block javascripts %}
    {% endblock %}
  </body>
</html>